# research.md — 작업·조사 기록

> CLAUDE.md 규칙: 모든 질문·요구·요청과 진행 과정·결과를 신중·깊이·상세·명확·정확하게 정리해 여기에 누적 기록한다.

## 기록 형식
- **날짜 — 제목**
  - 요청:
  - 진행:
  - 결정·근거:
  - 결과:

---

## 2026-07-13 — CLAUDE.md 규칙 추가 및 전 저장소 횡전개
- 요청: CLAUDE.md에 "모든 답변을 신중·깊이·상세·명확·정확하게 정리하고 research.md에 기록" 규칙 추가, 전 저장소 기본 브랜치에 횡전개.
- 진행: 7개 저장소(axax77, axdata_01/03/05/07/09, gax)의 기본 브랜치 CLAUDE.md에 "답변·기록 규칙" 섹션 추가, 각 저장소에 research.md 생성.
- 결정·근거: '횡전개' = 새 세션이 실제로 여는 기본 브랜치에 반영(ponytail 배포와 동일 기준). 기존 CLAUDE.md는 보존하고 규칙 섹션만 추가(수술적 변경).
- 결과: 각 저장소 커밋·푸시 완료(아래 커밋 참조).

## 2026-07-28 — 프로젝트 현황 점검 (Gim "07번 다시 브리핑")
- 요청: `axdata_07` 현황을 조사해 브리핑. 코드 변경 없는 점검.
- 진행: `README.md`·`package.json`·git 로그/상태 실측, 테스트 경로 존재 여부 확인.
- 발견 — **정체**: 과제 관리 시스템(Task Admin). Next.js App Router + `node:sqlite`(DB 서버·네이티브 패키지 불필요). 권한 2단계(운영자/매니저), 대시보드·필터·첨부링크. 실행은 `npm run seed` → `npm run dev`, 윈도우는 `과제관리-실행.bat` 더블클릭(자동 `git pull` 후 dev 서버). **`axdata_01`(엘드리아)의 애셋 목록 JSON을 과제로 자동 등록하는 임포터**(`scripts/import-asset-tasks.mjs`)로 게임 쪽과 연결됨.
- 발견 — **문제 3건**
  1. **`CLAUDE.md`가 커밋되지 않은 채 수정돼 있음**(`M`, +8/−34). 커밋본은 구판 **"실수 줄이기 9원칙"** 37줄이고, 작업 트리에는 상위 참조 스텁(11줄)이 들어와 있음. **이전 세션의 층 정리 작업이 커밋 없이 매달린 상태.**
  2. **테스트 명령이 존재하지 않는 경로**: 스텁의 `node --test system/test/*.test.mjs` → `system/test` 폴더 없음, `*.test.*` 파일 0개, `package.json`에 `test` 스크립트 없음(`dev`/`build`/`start`/`seed`뿐). **학습놀이_유아와 동일한 결함** — 작업공간 문서 7번의 예시 명령이 그대로 번진 실제 증거.
  3. **`research.md`에 프로젝트 작업 기록이 없음** — 07-13 규칙 횡전개 1건뿐. 그 이후 커밋(자동 동기화·선택형 질문 규칙·next 보안 패치 등)이 기록되지 않았음.
- **확인하지 못한 것**: 저장소 공개 여부(README에 초기 계정 `operator/admin1234`가 평문으로 있어, 공개라면 점검 필요) · DB 파일 위치와 시드 상태 · 실제 구동(서버를 띄우지 않았음) · `.next` 빌드 산출물의 최신성.
- 결과: **조사·기록만. 코드·설정 변경 0건.** 위 1·2번 조치는 Gim 결정 대기.
